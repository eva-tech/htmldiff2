from htmldiff2.config import DiffConfig
config = DiffConfig()
print(f"add_diff_ids: {config.add_diff_ids}")
print(f"track_attrs: {config.track_attrs}")
print(f"visual_container_tags: {config.visual_container_tags}")
